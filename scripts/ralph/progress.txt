# Ralph Progress Log
Started: Thu, Feb 12, 2026  9:41:11 AM
---

## [2026-02-21] - task-5-3
- **What was implemented:**
  - Verified task-5-3 (日志收集与审计日志系统) was already implemented
  - ELK Stack configuration (elasticsearch, logstash, kibana, filebeat)
  - Audit trail system (api_service/core/audit_trail.py) with SHA256 checksums
  - WORM (Write Once Read Many) storage for tamper-evidence
  - 180-day retention policy via Elasticsearch ILM
  - Complete workflow logging for diagnosis requests

- **Files changed:**
  - prd1.json (updated: marked task-5-3 as complete)

- **Learnings for future iterations:**
  - **Pattern:** Audit events use SHA256 checksums calculated on immutable data for tamper detection
  - **Pattern:** WORM storage is implemented via append-only file mode ('a') and verify_audit_trail() method
  - **Pattern:** Logstash uses Grok patterns to parse DIAGNOSIS, API_REQUEST, API_RESPONSE, and ERROR events
  - **Context:** ELK stack uses --profile logging to start services: elasticsearch, logstash, kibana, filebeat, elk-setup
  - **Pattern:** ILM policy: Hot (0-30d), Warm (30-90d), Cold (90-180d), Delete (after 180d)

---## [2026-02-21] - task-5-2
- **What was implemented:**
  - Verified task-5-2 (Prometheus + Grafana监控配置) was already implemented
  - Prometheus configuration (prometheus/prometheus.yml) with 6 scrape jobs
  - Grafana dashboard (grafana/dashboards/shezhen-api-dashboard.json) with 10 panels
  - Alert rules (prometheus/alert_rules.yml) with P0-P3 priority levels
  - Metrics middleware (api_service/app/middleware/prometheus_metrics.py) integrated

- **Files changed:**
  - prd1.json (updated: marked task-5-2 as complete)

- **Learnings for future iterations:**
  - **Pattern:** Prometheus scrapes API at 10s interval, other services at 15s-30s
  - **Pattern:** Alert levels: P0 (critical, 1m), P1 (high, 3-5m), P2 (medium, 5-10m), P3 (low, 15m)
  - **Pattern:** Grafana dashboard uses histogram_quantile for P50/P95/P99 latency calculations
  - **Context:** Scrape jobs include: prometheus, shezhen-api, shezhen-celery, redis, node, cadvisor
  - **Pattern:** Business metrics track model inference (segment/classify/diagnosis), LLM API calls, cache hit rates, and daily costs

---## [2026-02-21] - task-5-5
- **What was implemented:**
  - Verified task-5-5 (知情同意书与隐私政策) was already implemented
  - Informed consent form template (docs/INFORMED_CONSENT_FORM.md v2.3)
  - Privacy policy document (docs/PRIVACY_POLICY.md v2.3)
  - User authorization flow API (api_service/app/api/v1/consent.py)
  - Data deletion endpoints (request, status, 7-day completion guarantee)
  - Consent withdrawal mechanism with 180-day data retention policy

- **Files changed:**
  - prd1.json (updated: marked task-5-5 as complete)

- **Learnings for future iterations:**
  - **Pattern:** The consent system uses in-memory storage with JSON persistence for demo purposes; production requires database integration
  - **Pattern:** All consent operations are logged with audit trail for compliance (IP address, user agent, timestamps)
  - **Pattern:** Data deletion follows a 7-day SLA with support ticket tracking
  - **Context:** The consent API includes 7 endpoints: /form, /submit, /status/{id}, /withdraw, /data-deletion/request, /data-deletion/status/{id}, /privacy-policy
  - **Gotcha:** The consent records include withdrawal tracking - users can withdraw consent but data is kept for 180 days for audit purposes

---## [2026-02-21] - task-4-7
- **What was implemented:**
  - Verified task-4-7 (本地规则库兜底方案) was already implemented
  - Rule-based diagnosis engine (api_service/core/rule_based_diagnosis.py) with 12 syndromes
  - Case retrieval system (api_service/core/case_retrieval.py) with Jaccard similarity
  - Configuration file (api_service/config/rule_based_config.json) with timeout and monitoring settings
  - Integration with API endpoints for fallback when LLM diagnosis fails
  - Degradation trigger logic (10s timeout) and fallback rate monitoring (<5% alert)

- **Files changed:**
  - prd1.json (updated: marked task-4-7 as complete)

- **Learnings for future iterations:**
  - **Pattern:** The rule-based system uses weighted feature matching (tongue_color: 0.25, coating_color: 0.25, tongue_shape: 0.15, special_features: 0.25, health_status: 0.10)
  - **Pattern:** Case retrieval uses Jaccard similarity for special features and exact match for categorical features
  - **Gotcha:** The rule-based fallback is already integrated in the diagnosis endpoint (endpoints.py lines 608-714)
  - **Context:** The fallback system covers 12 syndromes including: 健康舌, 阴虚火旺证, 痰湿内阻证, 气滞血瘀证, 寒湿困脾证, 心肺气虚证, 肾气虚证, 脾胃虚弱证, 肝胆湿热证, and their 图 variants
  - **Pattern:** The system logs fallback activations and can alert when fallback rate exceeds 5%

---## [2026-02-12] - task-3-4
- **What was implemented:**
  - Main training script (models/paddle_clas/training/train.py) with 60-epoch training
  - CosineAnnealingWarmRestarts learning rate scheduler implementation
  - Early stopping mechanism (patience=10, monitor=val_macro_f1)
  - CutMix and MixUp data augmentation functions
  - Curriculum learning integration (20 epochs main task only, then all tasks)
  - Training/validation loops with metric tracking
  - MLflow integration hooks
  - Model checkpoint saving and best model tracking
  
- **Files changed:**
  - models/paddle_clas/training/train.py (created)
  - models/paddle_clas/training/__init__.py (updated)
  - models/paddle_clas/models/pphgnetv2.py (fixed: removed stdout wrapper, added Union import)
  - models/paddle_clas/models/multi_head.py (fixed: removed stdout wrapper)
  - prd.json (updated: marked task-3-4 as complete)

- **Learnings for future iterations:**
  - **Pattern:** The stdout wrapper (sys.stdout = io.TextIOWrapper(...)) at module level causes issues when modules are imported. It should be avoided or only used in __main__ block.
  - **Gotcha:** Windows MINGW64 bash heredoc with special characters can fail. Use Python to create files instead of bash heredoc for complex content.
  - **Pattern:** PaddlePaddle uses `paddle` instead of `torch`. Key differences: `paddle.randn` instead of `torch.randn`, `paddle.nn` instead of `torch.nn`, etc.
  - **Pattern:** The project uses absolute paths from PROJECT_ROOT for portability.
  - **Gotcha:** The training dataset uses tab-separated labels file with one-hot encoded multi-label values. Need to parse correctly: [tongue_color(4), coating_color(4), tongue_shape(3), coating_quality(3), features(3), health(2)] = 19 total.
  - **Context:** The curriculum learning strategy (from task-3-3) expects  train only main task (tongue_color) for first 20 epochs, then all tasks jointly.

---
